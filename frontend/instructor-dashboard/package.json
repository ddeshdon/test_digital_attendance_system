{
  "name": "instructor-dashboard",
  "version": "1.0.0",
  "description": "Digital Attendance System - Instructor Dashboard",
  "private": true,
  "dependencies": {
    "@ant-design/icons": "^5.2.6",
    "ajv": "^8.12.0",
    "antd": "^5.10.0",
    "axios": "^1.5.0",
    "moment": "^2.29.4",
    "qrcode.react": "^3.1.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-scripts": "5.0.1",
    "web-vitals": "^3.4.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "deploy": "npm run build && aws s3 sync build/ s3://attendance-dashboard-bucket --delete"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "@testing-library/jest-dom": "^6.1.0",
    "@testing-library/react": "^13.4.0",
    "@testing-library/user-event": "^14.4.3"
  },
  "resolutions": {
    "ajv": "8.12.0"
  },
  "overrides": {
    "ajv": "8.12.0"
  },
  "proxy": "http://localhost:5000"
}
